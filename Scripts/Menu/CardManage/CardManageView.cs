public class CardManageView : ViewBase
{
    // Start is called before the first frame update
    public void Start()
    {
        hide();
    }

    // Update is called once per frame
    public void Update()
    {

    }
}
